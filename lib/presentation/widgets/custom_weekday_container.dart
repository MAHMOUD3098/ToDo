import 'package:flutter/cupertino.dart';

class CustomWeekDayContainer extends StatelessWidget {
  const CustomWeekDayContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
